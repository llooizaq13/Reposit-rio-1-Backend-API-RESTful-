# Pokemon Backend (Express + Prisma + MySQL)

## Requisitos
- Node >= 16
- MySQL rodando (local ou remoto)
- npm

## Setup
1. Clone o repositório
2. Copie `.env.example` para `.env` e atualize `DATABASE_URL`
3. Instale dependências:
